server:
  port: 8001

spring:
  application:
    name: service-store
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root

  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true